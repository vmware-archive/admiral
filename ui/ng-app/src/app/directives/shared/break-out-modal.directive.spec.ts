import { BreakOutModalDirective } from './break-out-modal.directive';

describe('BreakOutModalDirective', () => {
  it('should create an instance', () => {
    const directive = new BreakOutModalDirective();
    expect(directive).toBeTruthy();
  });
});
