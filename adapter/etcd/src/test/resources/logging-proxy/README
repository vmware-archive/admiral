HTTP proxy that logs the contents of every request and response.
Run:
npm intall
node app.js ${TARGET_ADDRESS}

eg:
node app.js http://192.168.99.104:4001
node app.js http://localhost:8282

Note:
if using this with Admiral Etcd emulator, make sure to set
-Ddev.mode.etcd.client.url=YOUR_IP:PORT, e.g. 192.168.1.3:6443